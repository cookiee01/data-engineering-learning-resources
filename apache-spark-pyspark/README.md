# PySpark (Local macOS) — Practice Track

Primary doc:
- `PYSPARK_QA_JOURNEY.md` (Q&A + tasks + code in one place)

Support files:
- `data/` small practice datasets
- `output/` generated outputs

Setup:
```bash
cd /Users/arpitsingh/MyWorkingDir/PycharmProjects/data-engineering-learning-resources/apache-spark-pyspark
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Run:
```bash
source .venv/bin/activate
python PYSPARK_QA_JOURNEY.md  # (No, this is a markdown file)
```

Instead:
- Copy/paste each code block from `PYSPARK_QA_JOURNEY.md` into a Python file, OR
- Tell me and I’ll generate runnable scripts for each Q&A section.
