# Module 5 — Aggregations + Window Functions

## Goal
- `groupBy` + `agg`
- Window functions for ranking, running totals, dedup

## Window mental model
A window is “grouping + ordering + a frame”, computed per row.

Run:
- `code/07_aggs_and_windows.py`
